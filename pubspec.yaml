name: audio_service_refactor
description: Refactored example of audio_service

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.0.1
  audio_session: ^0.1.2
  just_audio: ^0.7.5
  flutter_tts: ^3.0.0
  rxdart: ^0.27.0
  audio_service: ^0.17.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
